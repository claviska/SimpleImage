language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

matrix:
  allow_failures:
    - php: 5.4
    - php: 5.5
    - php: 7.0
    - php: hhvm

matrix:
  fast_finish: true

script:
  - composer update-all
  - composer test

after_script:
  - composer coveralls
